export const ReactSelectStyles = {
    control: (base) => ({
        ...base,
        borderWidth: '2px',
        borderColor: '#e8edff',
    }),
    container: (base) => ({
        ...base,
        marginBottom: '12px',
    }),
};
